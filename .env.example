# Public API base URL used by the browser. Leave empty for same-origin behind nginx.
VITE_API_URL=

# Optional backend target for `pnpm dev` proxying.
# Example: http://127.0.0.1:8082
VITE_API_PROXY_TARGET=
